# Repository guidance

- After each requested change is complete and verified, create a Git commit containing that change and push it to the configured remote before starting the next change.
- Keep unrelated or pre-existing worktree changes out of the commit.
